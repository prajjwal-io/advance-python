number_list = range(-7,15)

greater_than_zero = list(filter(lambda x: x > 0, number_list))  # filter out all numbers less than zero
print(greater_than_zero)

#list of zero
zeroes = [0]*5
print(zeroes)